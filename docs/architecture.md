# Architecture

## Overview

Orpheus is split into four main projects.

- Orpheus.Core
- Orpheus.Adapters
- Orpheus.Api
- Orpheus.Cli

The dependency direction points inward toward Core.

## Orpheus.Core

Core contains domain models and interfaces.

Responsibilities:

- persona model
- speech request model
- speech result model
- audio result model
- transformation interfaces
- TTS interfaces
- cache interfaces
- navigation event model later

Core must not depend on:

- Adapters
- Api
- Cli
- file system
- network
- concrete AI providers
- concrete TTS providers

If Core needs behavior implemented by another project, Core defines a small interface and the adapter layer implements it.

## Adapter Layer

The project name is Orpheus.Adapters because it contains concrete adapters for Core interfaces.

Initial responsibilities:

- stub persona transformer
- stub TTS provider
- deterministic WAV audio provider
- optional Windows SAPI process provider
- in-memory persona repository for tests and simple development
- file-based persona repository
- committed sample persona loading
- local persona override loading
- persona runtime metadata resolution
- last original text state store
- audio cache later

Future responsibilities:

- voice asset resolver
- voice identity store
- audio cache implementation
- provider process adapter
- local LLM provider
- local TTS provider
- remote TTS provider
- provider configuration
- audio processing

## Orpheus.Api

API exposes HTTP endpoints for clients.

Initial endpoint:

- POST /speak

Future endpoints:

- GET /personas
- POST /transform
- POST /synthesize
- POST /navigation/instruction
- POST /story/continue

## Orpheus.Cli

CLI exposes local command-line access.

Initial command:

- speak a text with a selected persona and print transformed text plus audio URI

Future commands:

- voice status
- voice regenerate
- voice accept
- voice reject

## Main Flow

1. Client sends text and persona id.
2. API or CLI creates a speech request.
3. Persona repository resolves the persona.
4. Persona transformer creates persona-specific text.
5. The TTS adapter resolves the active local voice identity for the persona.
6. The TTS adapter creates or stubs audio output. The default development provider writes a deterministic WAV file under `.orpheus/audio`.
7. Audio cache stores or reuses generated audio.
8. Response returns transformed text and optional audio location.

## Core Interfaces

Initial interfaces:

- IPersonaRepository
- IPersonaTransformer
- ITextToSpeechProvider
- IAudioCache
- ILastSpeechTextStore

Future interfaces may include:

- IVoiceIdentityStore
- IPersonaRuntimeMetadataStore

Core owns the abstractions and domain language. The adapter layer owns file paths, provider process execution, local runtime state, and concrete persistence.

## Persona Sources

Committed sample personas are text-only. They may include fields such as `id`, `displayName`, `description`, `previewText`, `speech.style`, `voice.provider`, `voice.voiceId`, and `voice.style`.

Local/private personas are loaded from `.orpheus/personas` when that folder exists. Additional local persona paths may later be supplied through `ORPHEUS_PERSONA_PATHS`.

Local personas override committed personas with the same `id`. The active source should be visible in diagnostics so users can see which definition won.

Committed persona sources must reject runtime asset references, local paths, provider secrets, samples, models, generated media, and other protected assets. Local persona sources may include optional `voice.assets` data for the user's own runtime environment.

`voice.assets` is not part of the Core `Persona` model. The persona loader reads it once and stores it as adapter-layer runtime metadata for the selected provider.

Supported local `voice.assets` fields are `speakerSample`, `referenceAudio`, `modelPath`, `speakerEmbedding`, and `providerSettings`. Relative asset paths resolve relative to the local persona file. Asset existence is intentionally deferred until synthesis time because local provider setup may be incomplete during application startup.

## Voice Runtime

A real provider must preserve voice identity across requests for the same persona. If a local persona supplies `voice.assets`, those assets take precedence. If no assets are supplied, a persona-capable provider may generate or materialize a voice identity once and store it in an ignored local runtime folder.

Voice identities have a small lifecycle:

- `active`: used for normal synthesis
- `candidate`: generated by regeneration and awaiting user acceptance
- `rejected`: no longer wanted and eligible for cleanup

The first `speak` call may generate and activate a voice automatically. Regeneration creates a candidate and preview audio. Accepting a candidate makes it active and can clean up old candidates.

Each voice identity should include a fingerprint from the persona id, provider, voice id, voice style, relevant provider settings, and local asset metadata. For V1, asset metadata means normalized path, file size, and last modified time rather than full file hashes.

If the current persona no longer matches the stored voice fingerprint, the voice is stale. V1 may continue using it, but must warn clearly.

## Preview and Local State

Voice regeneration needs preview text. The selection order is:

1. explicit CLI/API text
2. last original synthesis text for the persona
3. persona `previewText`
4. global preview text from app configuration
5. clear error

Only the last original synthesis text is stored. Transformed persona output is not needed for regeneration preview. The state is local runtime data under `.orpheus/state`, enabled by default, and configurable through `Orpheus:State:StoreLastOriginalText`.

On Unix-like systems the file store attempts user-only file permissions. On Windows it relies on the existing user/workspace ACLs and does not broaden access.

## Safety Boundary

Voice models, samples, generated audio, proprietary assets, and secrets are runtime data.

They must not be committed to the repository.

The repository may provide text-only persona presets, including presets that name their inspirations. It must not provide protected audio, extracted assets, trained voice models, generated media, or secrets.
